
@{
    ViewBag.Title = "Index";
}

<h2>Categories</h2>
<div>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>CategoryId</th>
                <th>CategoryName</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var i in ViewBag.Categories)
            {
                <tr>
                    <td>@i.CategoryId</td>
                    <td>@i.CategoryName</td>
                    <td>
                        @Html.ActionLink("Edit", "NewCategory", new { id = i.CategoryId }, new { @class = "btn btn-warning btn-xs" })
                        @Html.ActionLink("Del", "Delete", new { id = i.CategoryId }, new { @class = "btn btn-danger btn-xs" })
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <nav>
        <ul class="pagination">
            @for (int i = 0; i < ViewBag.CountOfPages; i++)
            {
                <li class="page-item">
                    @Html.ActionLink((i + 1).ToString(), "Index", new { id = i + 1 }, new { @class = "page-link" })
                </li>
            }
        </ul>

    </nav>
    @Html.ActionLink("New category", "NewCategory", null, new { @class = "btn btn-success", @style = "margin-bottom:10px;" })
    <div>
        @Html.ActionLink("Good", "ShowGood", null, new { @class = "btn btn-warning", @style = "margin-right:10px;" })
        @Html.ActionLink("Manufacturer", "ShowManufacturer", null, new { @class = "btn btn-warning" })
    </div>

</div>
